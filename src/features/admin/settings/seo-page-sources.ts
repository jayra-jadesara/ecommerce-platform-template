/** Shared SEO admin types (safe for client + server). */

export type PageSeoSourceInfo = {
  key: string;
  label: string;
  path: string;
  sourceTitle: string;
  sourceDescription: string;
  sourceHint: string;
};
