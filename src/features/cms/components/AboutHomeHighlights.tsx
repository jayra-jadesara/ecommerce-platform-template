/**
 * @deprecated Homepage About blocks are configured via
 * Content → Homepage → Other information (`other_information` section).
 * Kept so old imports compile; returns null.
 */
export async function AboutHomeHighlights(_props: {
  headingHighlightStyle?: string | null;
}) {
  return null;
}
